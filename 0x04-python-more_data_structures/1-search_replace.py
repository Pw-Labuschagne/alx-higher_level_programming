#!/usr/bin/python3
def search_replace(my_lsit, search, replace):

    alist = mylist[:]

    for x, y in enumerate(alist):
        if alist[i] = search:
            alist[i] = replace

    return alist
